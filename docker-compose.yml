version: "2"

services:
    web:
        image: dyalog/dyalog:odbc
        container_name: dcms_web
        volumes:
        - ${PWD}:/app
        - ${PWD}/data:/data
        - ${PWD}/secrets:/secrets
        tty: true
        env_file: env
    db:
        image: mariadb
        container_name: dcms_db
        volumes:
            - ${PWD}/dcms_db:/var/lib/mysql
        env_file: env
        tty: true
