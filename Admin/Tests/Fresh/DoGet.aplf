 r←expected DoGet path;HTTP;code;h;type;url
⍝ expected: (code: 0 ⋄ type: ?)
⍝ type is an empty array of the same shape and type as the expected result
 (code type)←expected ⎕VGET'code' 'type'
 url←⊃(⊣,':',⊢)/##.##.GetEnv'service_url' 'service_port'
⍝ Make HTTP request
 HTTP←##.##.##.HttpCommand
 h←HTTP.New'GET'(url,path)
 h.TranslateData←1
⎕← r←h.Run
⍝ Test against expected result
