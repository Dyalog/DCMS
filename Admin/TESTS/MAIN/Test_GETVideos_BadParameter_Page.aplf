 Test_GETVideos_BadParameter_Page←{
⍝ Check GET /videos rejects a non-numeric page with 400
     url←⍵
     h←##.##.##.HttpCommand.New'GET'
     h.URL←url,'/videos?per_page=20&page=poo'
     _←'origin'h.SetHeader ##.##.GetEnv'URL'
     res←h.Run
     400=res.HttpStatus
 }
