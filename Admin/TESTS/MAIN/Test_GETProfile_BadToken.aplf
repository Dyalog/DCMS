 Test_GETProfile_BadToken←{
⍝ Check GET /admin/profile rejects a wrong api key with 401
     h←##.##.##.HttpCommand.New'GET'
     h.URL←⍵,'/admin/profile'
     _←'x-api-key'h.SetHeader'wrong'
     res←h.Run
     401=res.HttpStatus
 }
