 Test_GETVideos_BadParameter_PerPage←{
⍝ Check GET /videos rejects a non-numeric per_page with 400
     url←⍵
     h←##.##.##.HttpCommand.New'GET'
     h.URL←url,'/videos?per_page=ILLEGAL'
     _←'origin'h.SetHeader ##.##.GetEnv'URL'
     res←h.Run
     400=res.HttpStatus
 }
