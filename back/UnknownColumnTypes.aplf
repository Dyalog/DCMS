 UnknownColumnTypes←{
     c←⍵∊⊂'' ⋄ t←∨/¨r ⋄ rc←-∨/t         ⍝ empty columns; tables containing unknown types; return code (0 if none, ¯1 otherwise)
     0=rc:0 ''                          ⍝ All types known
     (ut uc)←t∘/¨(⊂r)/¨¨types columns   ⍝ unknown types;  unknown columns
     UnknownTypeInColumn←{0∊⍴⍵:'' ⋄ ∊'Unknown type (',⍺,') in column (',⍵,')'}
     InTable←{⍺,' in table (',⍵,')'}
     msg←(ut UnknownTypeInColumn¨uc)InTable¨t/names
     (rc)(msg)
 }
