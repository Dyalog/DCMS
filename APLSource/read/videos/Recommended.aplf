 r←Recommended req;Q;Values;all_id;fid;fields;n;rec;schemas;this;vids;ytid
⍝ List of video objects most related to video with youtube_id given by "v" query parameter

 :If '⍠SPEC'≡req
     schemas←##.##.spec.components.schemas
     r←(tags:,⊂'Videos' ⋄ summary:'Get video recommendations' ⋄ operationId:'read.videos.Recommended'
         parameters:(
             (name:'youtube_id' ⋄ in:'path' ⋄ required:⊂'true' ⋄ schema:(type:'string') ⋄ description:'11-character YouTube video ID')
             (name:'n' ⋄ in:'query' ⋄ required:⊂'false' ⋄ schema:(type:'integer' ⋄ minimum:1 ⋄ default:5) ⋄ description:'Number of recommendations to return')
         )
         responses:(
             ⍙200:(
                 description:'List of recommended videos'
                 content:(⍙application⍙47⍙json:(schema:(type:'array' ⋄ items: schemas.video)))
             )
             ⍙404: (
                 description:'Video not found'
                 content:(⍙application⍙47⍙json:(schema: schemas.error))
             )
         )
     )
     :Return
 :EndIf

 Q←{⊃(req.QueryParams⍪⊂'')[req.QueryParams[;1]⍳⊂,⍵;2]}
 ytid←Q'v'
 n←{0∊⍴⍵:1 ⋄ 2⊃⎕VFI ⍵}Q'n'   ⍝ Number of results. Default 1.
 vids←##.##.CACHE.videos
 Values←#.isolate.Values
 vids.recommended←⊃vids.recommended Values'##.recommended'   ⍝ Use existing values unless the ComputeRecommended isolate has finished processing
 all_id←vids.index_cols⊃⍨vids.fields⍳⊂'youtube_id'
 :If 0∊⍴ytid
     r←req.Fail 422
 :ElseIf (≢ytid)=⊢/⍴all_id
     this←all_id⍳ytid
 :AndIf this≤≢vids.values
     fields←'title' 'event' 'event_shortname' 'presented_at' 'category' 'url' 'youtube_id' 'thumbnail' 'presenter'
     fid←vids.fields⍳fields
     rec←vids.recommended[this;⍳n]
     r←##.Table2JSON fields⍪##.##.CACHE.videos.values[rec;fid]
 :Else
     r←req.Fail 404   ⍝ Video not found
 :EndIf
