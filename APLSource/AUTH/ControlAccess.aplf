 ControlAccess←{
     restricted←⊆'/crud' '/teapot'
     ∨/restricted(⊃⍷)¨⊂⍵.Endpoint:r⊣##.Log'[AUTH] ',('allow' 'deny'⊃⍨1+r←VerifyToken ⍵),' ',⍵.Endpoint
     0 ⍝ 0 means success, continue
 }
